function formulascirculo(radio) {
    return Math.PI *radio*radio;
}
function formularectangulos(l1,l2) {
    return l1*l2;
}
     console.log ("El área de un círculo de radio 2 es " + formulascirculo(2));
     console.log ("El área de un círculo de rectangulo es " + formularectangulos(3, 4));

   